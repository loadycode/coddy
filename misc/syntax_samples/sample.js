// coddy by loadycode
// graphite00050
// gnu general public license v3.0

// code samples. javascript

let number = 10;

var string = 'hello';

const float = 3.41;

function func (arg) {
	console.log (arg);
}

class objects {
	constructor (arg) {
		console.log (arg);
	}
}